// 	Utilizando os conceitos que estudamos, vamos criar um programa que funcionará como um
//  sinal de transito.

let cor = 'verde';

if (cor === 'vermelho') {
    console.log('PARE!');
} else if (cor === 'amarelo') {
    console.log('ATENÇÃO!')
} else {
    console.log('SIGA EM FRENTE!')
}