// Escreva um programa que receba um valor em metros e 
// o exiba no console convertido em milímetros.

function conversao (metros) {
    let resultado = metros*1000;
    console.log(`${metros} metro(s) equivale à ${resultado} milímetros`);
}
conversao(2)